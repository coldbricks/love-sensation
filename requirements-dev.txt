# Synthetic safety, crop, and fake-player audio tests; no detector weights needed.
# Install torch/torchvision through Setup.ps1 or the explicit index in CI first.
pytest==9.1.1
Pillow>=11
numpy>=2
PySide6==6.11.2
